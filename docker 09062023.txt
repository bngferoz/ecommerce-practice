sudo docker insptect imageid
sudo docker run --name redis-latest -p6379:6379 -d redis-latest
(d means deamon mood)
sudo docker inspect cId
sudo docker logs cId
sudo docker exec -it cId /bin/sh
(it means interactive mood)
ls
pwd
cd ..
ls
redis cli

keys *
exit
exit
docker stop cid
docker rm
docker rmi 
docker system prune -a
mvn clean
mvn package

docker build -t bngferoz/vip-service-registry:0.0.1 . 

docker images
docker run -d -p8761:8761 --name serviceregistry imageid
sudo touch Dockerfile
sudo chmod a+rwx /path/to/file
mvn clean install
docker build -t bngferoz/vip-config-server:0.0.1 .
docker run -d -p9296:9296 --name configserver imageid
docker logs cId
docker stop cId

docker rm cId
sudo ifconfig -a
http://192.168.31.53:8761/

sudo apt update
sudo apt install openssh-server
sudo systemctl status ssh
--enough--
--if does not work--
--enable sharing from settings
--sudo ufw allow from any to any port 5900 proto tcp
--unlock users from settings
--localhost(172.17.0.1)
sudo docker build -t bngferoz/vip-cloud-gateway:0.0.1 .

sudo docker run -d -p9090:9090 --name cloudgateway imageId
docker login
sudo docker push bngferoz/vip-service-registry:0.0.1

sudo docker stop cid cid cid
sudo docker system prune -a

sudo touch docker-compose.yml